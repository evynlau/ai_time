"""
🎯 Day 12 練習題：while 迴圈闖關遊戲！
================================================================================

小朋友們，歡迎來到 while 迴圈闖關遊戲！🎮
請認真完成每一關的題目，將你的答案寫在「你的程式碼」區塊。
完成後可以執行這支程式來測試你的答案是否正確！✨

================================================================================
"""

# 讓我們先引入一些需要用到的模組
import sys


def 測試判定(題目名稱, 學生答案, 正確答案):
    """
    這是一個測試判定函式
    如果學生的回傳值等於正確答案，顯示 ✅ 正確！
    否則顯示 ❌ 錯誤，並告訴他正確答案是什麼
    """
    if 學生答案 == 正確答案:
        print(f"✅ {題目名稱}：正確！太棒了！🎉")
        return True
    else:
        print(f"❌ {題目名稱}：再想想看喔！")
        print(f"   你的答案：{學生答案}")
        print(f"   正確答案：{正確答案}")
        return False


# ================================================================================
# 🌟 第一關：基本語法挑戰 🌟
# ================================================================================

def 第一關():
    """
    📚 題目：請回答下列問題

    1. while 迴圈和 for 迴圈最大的差別是什麼？
       什麼時候應該用 while 而不是 for？

    2. 請說明為什麼這個程式會造成「無限迴圈」？

       count = 1
       while count <= 10:
           print(count)
       # 沒有 count += 1

    3. break 關鍵字的作用是什麼？
    """
    print("\n" + "=" * 50)
    print("🌟 第一關：觀念問答題 🌟")
    print("=" * 50)

    # 請填空（將你的答案填在引號內）

    答案1 = ""  # while 迴圈適用時機（字描述）
    答案2 = ""  # 為什麼會無限迴圈（字描述）
    答案3 = ""  # break 的作用（簡短描述）

    # 示範用（實際教學時老師會根據情況評分）
    # 這裡示範正確的觀念，學生的回答應該要類似
    示範答案1 = "當不知道要執行幾次，只知道何時該停止時，應使用 while 迴圈"
    示範答案2 = "因為 count 沒有改變，永遠停留在 1，所以 count <= 10 永遠為 True"
    示範答案3 = "break 可以強制跳出迴圈，類似緊急煞車"

    print("\n📝 你的答案：")
    print(f"   1. {答案1}")
    print(f"   2. {答案2}")
    print(f"   3. {答案3}")

    # 這裡用示範答案做測試（實際使用時請註解掉）
    # 測試判定("第一關", 答案1, 示範答案1)


# ================================================================================
# 🌟 第二關：計數器 while 迴圈 🌟
# ================================================================================

def 第二關():
    """
    📚 題目：使用 while 迴圈，請寫一個函式

    函式名稱：倒數計時機
    功能：從指定的數字倒数到 1，每次印出 "⏰ X"
         當數到 1 的時候，印出 "🎉 時間到！"

    提示：
    - 這個函式接受一個參數：start（起始數字）
    - 使用 while 迴圈
    - 不要忘記改變計數器的值！

    範例輸出（呼叫 倒數計時機(3)）：
       ⏰ 3
       ⏰ 2
       ⏰ 1
       🎉 時間到！
    """
    print("\n" + "=" * 50)
    print("🌟 第二關：計數器 while 迴圈 🌟")
    print("=" * 50)

    # 請在下面寫入你的程式碼
    def 倒數計時機(start):
        # 你的程式碼從這裡開始
        pass
        # 你的程式碼到這裡結束

    # 測試
    print("\n📝 測試：倒數計時機(5)")
    倒數計時機(5)
    # 預期輸出：
    # ⏰ 5
    # ⏰ 4
    # ⏰ 3
    # ⏰ 2
    # ⏰ 1
    # 🎉 時間到！


# ================================================================================
# 🌟 第三關：總和計算器 🌟
# ================================================================================

def 第三關():
    """
    📚 題目：使用 while 迴圈，請寫一個函式

    函式名稱：計算總和
    功能：計算 1 + 2 + 3 + ... + n 的總和
         並回傳這個總和

    提示：
    - 這個函式接受一個參數：n
    - 使用 while 迴圈搭配計數器
    - 需要一個變數來記錄「總和」

    範例：
       result = 計算總和(5)
       print(result)  # 輸出：15（因為 1+2+3+4+5 = 15）

       result = 計算總和(10)
       print(result)  # 輸出：55（因為 1+2+...+10 = 55）
    """
    print("\n" + "=" * 50)
    print("🌟 第三關：總和計算器 🌟")
    print("=" * 50)

    # 請在下面寫入你的程式碼
    def 計算總和(n):
        # 你的程式碼從這裡開始
        pass
        # 你的程式碼到這裡結束

    # 測試
    print("\n📝 測試：")
    result1 = 計算總和(5)
    print(f"   計算總和(5) = {result1}，預期：15")
    測試判定("第三關-1", result1, 15)

    result2 = 計算總和(10)
    print(f"   計算總和(10) = {result2}，預期：55")
    測試判定("第三關-2", result2, 55)

    result3 = 計算總和(100)
    print(f"   計算總和(100) = {result3}，預期：5050")
    測試判定("第三關-3", result3, 5050)


# ================================================================================
# 🌟 第四關：找出倍數 🌟
# ================================================================================

def 第四關():
    """
    📚 題目：使用 while 迴圈，請寫一個函式

    函式名稱：找倍數
    功能：找出從 1 到 max 中，所有可以被 divisor 整除的數
         把這些數存成一個串列（list）並回傳

    提示：
    - 這個函式接受兩個參數：divisor 和 max
    - divisor = 要找的倍數
    - max = 搜尋範圍的上限（包含）
    - 使用 while 迴圈檢查每個數字是否能被整除

    範例：
       result = 找倍數(3, 20)
       print(result)  # 輸出：[3, 6, 9, 12, 15, 18]

       result = 找倍數(5, 30)
       print(result)  # 輸出：[5, 10, 15, 20, 25, 30]
    """
    print("\n" + "=" * 50)
    print("🌟 第四關：找出倍數 🌟")
    print("=" * 50)

    # 請在下面寫入你的程式碼
    def 找倍數(divisor, max):
        # 你的程式碼從這裡開始
        pass
        # 你的程式碼到這裡結束

    # 測試
    print("\n📝 測試：")
    result1 = 找倍數(3, 20)
    print(f"   找倍數(3, 20) = {result1}，預期：[3, 6, 9, 12, 15, 18]")
    測試判定("第四關-1", result1, [3, 6, 9, 12, 15, 18])

    result2 = 找倍數(5, 30)
    print(f"   找倍數(5, 30) = {result2}，預期：[5, 10, 15, 20, 25, 30]")
    測試判定("第四關-2", result2, [5, 10, 15, 20, 25, 30])


# ================================================================================
# 🌟 第五關：使用者輸入驗證 🌟
# ================================================================================

def 第五關():
    """
    📚 題目：使用 while 迴圈，請寫一個函式

    函式名稱：取得正確年齡
    功能：
    - 持續要求使用者輸入年齡
    - 如果輸入的不是有效的數字（0-150），就說「無效的年齡，請重新輸入」
    - 如果年齡在範圍內，就回傳這個年齡（轉成整數）

    提示：
    - 使用 while True 開始無窮迴圈
    - 用 try/except 來檢查輸入是否為數字（可以參考以下範例）
    - 或者用 .isdigit() 來判斷是否為數字字串
    - 有效範圍是 0 到 150（包含）
    - 遇到正確輸入時用 break 結束迴圈

    這個題目比較難，可以先跳過，以後再回來做！

    範例互動：
       請輸入年齡：abc
       ❌ 無效的年齡！
       請輸入年齡：200
       ❌ 無效的年齡！
       請輸入年齡：15
       ✅ 有效！年齡是 15
       （函式回傳 15）
    """
    print("\n" + "=" * 50)
    print("🌟 第五關：使用者輸入驗證 🌟")
    print("=" * 50)

    # 這個函式需要使用者輸入，為了不影響自動化測試
    # 我們用一個「模擬輸入」的方式來測試
    # 實際上課時可以移除這段測試程式碼

    def 取得正確年齡():
        # 你的程式碼從這裡開始
        pass
        # 你的程式碼到這裡結束

    # 由於這個函式需要手動輸入，我們跳過自動測試
    print("\n📝 這個題目需要手動測試")
    print("   請自行執行 取得正確年齡() 並嘗試不同輸入！")


# ================================================================================
# 🌟 第六關：猜數字遊戲 🌟
# ================================================================================

def 第六關():
    """
    📚 題目：使用 while 迴圈，請寫一個函式

    函式名稱：猜數字遊戲
    功能：
    - 電腦想一個 1 到 10 的隨機數字（可以自己指定一個當答案）
    - 玩家有 3 次機會猜測
    - 每次猜測後，電腦要告訴玩家「太大」或「太小」或「對了！」
    - 如果 3 次都沒猜到，印出「遊戲結束！正確答案是 X」

    提示：
    - 這個函式接受一個參數：正確答案
    - 用 while 迴圈搭配剩餘次數計數器
    - 每次猜測後要減少剩餘次數

    範例（假設正確答案是 7）：
       猜數字遊戲(7)
       輸出：
       🔮 請猜一個 1-10 的數字：5
       📈 太小了！再大一點！
       🔮 請猜一個 1-10 的數字：9
       📉太大了！再小一點！
       🔮 請猜一個 1-10 的數字：7
       🎉 恭喜猜對了！正確答案是 7！
    """
    print("\n" + "=" * 50)
    print("🌟 第六關：猜數字遊戲 🌟")
    print("=" * 50)

    # 請在下面寫入你的程式碼
    def 猜數字遊戲(正確答案):
        # 你的程式碼從這裡開始
        pass
        # 你的程式碼到這裡結束

    # 測試（用數字 7 作為正確答案）
    print("\n📝 測試：猜數字遊戲(7)")
    print("   （說明：這個遊戲需要手動輸入猜測數字）")
    print("   （正確答案是 7，請嘗試猜測！）\n")
    # 猜數字遊戲(7)


# ================================================================================
# 🌟 第七關：找出質數 🌟（挑戰題）
# ================================================================================

def 第七關():
    """
    📚 題目：使用 while 迴圈，請寫一個函式

    函式名稱：找出質數
    功能：找出所有小於 n 的質數，回傳一個串列

    提示：
    - 質數是「大於 1 的自然數，且只能被 1 和自己整除」
    - 例如：2, 3, 5, 7, 11, 13, 17, 19, 23...

    範例：
       result = 找出質數(20)
       print(result)  # 輸出：[2, 3, 5, 7, 11, 13, 17, 19]
    """
    print("\n" + "=" * 50)
    print("🌟 第七關：找出質數（挑戰題）🌟")
    print("=" * 50)

    # 請在下面寫入你的程式碼
    def 找出質數(n):
        # 你的程式碼從這裡開始
        pass
        # 你的程式碼到這裡結束

    # 測試
    print("\n📝 測試：")
    result = 找出質數(20)
    print(f"   找出質數(20) = {result}，預期：[2, 3, 5, 7, 11, 13, 17, 19]")
    測試判定("第七關", result, [2, 3, 5, 7, 11, 13, 17, 19])


# ================================================================================
# 主程式：執行所有關卡
# ================================================================================

def main():
    print("=" * 60)
    print("🎮 Day 12：while 迴圈闖關遊戲 🎮")
    print("=" * 60)

    # 依序執行每一關（可以把不想執行的關卡註解掉）
    第一關()
    第二關()
    第三關()
    第四關()
    第五關()  # 需要手動測試
    第六關()  # 需要手動測試
    第七關()  # 挑戰題

    print("\n" + "=" * 60)
    print("🎉 恭喜完成所有關卡！🎉")
    print("=" * 60)
    print("💡 提示：如果有些題目不會，可以先跳過，稍後再回來挑戰！")
    print("📚 參考答案在 answer.py 檔案中。")


if __name__ == "__main__":
    main()
